spring:
  application:
    name: Y TE

  datasource:
    url: jdbc:postgresql://localhost:5432/helloword
    username: postgres
    password: 12345
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      max-lifetime: 1800000
      idle-timeout: 600000

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

jwt:
  signer-key: HJ4ppveef04BKJzTq+dyLhRdicqYiQWxaSqTLiTUxksDu4zMs/NLfrJsbHz+4OiB
  expiration-duration: 300 #don vi giay

springdoc:
  api-docs:
    path: "/api/v1/api-docs"
  swagger-ui:
    operationsSorter: method
    try-it-out-enabled: true
    path: "/api/swagger-ui.html"
  servers: http://localhost:8080
