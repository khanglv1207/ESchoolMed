spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/eSchoolMed
    username: postgres
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    enabled: true
